{
  "name": "keychanger-backend",
  "version": "1.0.0",
  "description": "KeyChanger Backend API",
  "main": "app/main.py",
  "scripts": {
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
